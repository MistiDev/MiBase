# MiBase

* Easy install
* Easy to use
* JSON Database

# Install

```
npm i https://github.com/MistiDev/MiBase
```

# Setup

```js
const MiBase = require("mibase")
const db = new MiBase({
  path: './MiBase',
  tables: ['main']
})
```
